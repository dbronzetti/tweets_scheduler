setup:
  addons:
  - plan: heroku-postgresql
    as: db
  - plan: heroku-redis
    as: redis

build:
  docker:
    web: Dockerfile

run:
  web: bundle exec rails s
  sidekiq:
    command:
      - bundle exec sidekiq
    image: web

release:
  command:
    - bin/rails db:migrate
  image: web
